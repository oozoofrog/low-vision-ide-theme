# Ghostty

## 설치

1. `ghostty/glareguard-dark`, `ghostty/glareguard-light` 파일을 `~/.config/ghostty/themes/`로 복사합니다.
2. `~/.config/ghostty/config`에 아래를 추가합니다.

```conf
theme = glareguard-dark
```

라이트 모드를 쓰려면:

```conf
theme = glareguard-light
```
