# Xcode

## 설치

1. `xcode/GlareGuard Dark.xccolortheme`, `xcode/GlareGuard Light.xccolortheme` 파일을 아래 폴더로 복사합니다.
   - `~/Library/Developer/Xcode/UserData/FontAndColorThemes/`
2. Xcode 재시작 후 `Settings/Preferences → Themes`에서 `GlareGuard Dark`를 선택합니다.
3. 라이트 모드를 쓰려면 `GlareGuard Light`를 선택합니다.
